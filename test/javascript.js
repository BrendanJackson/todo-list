/*

function buttonClicked() {
    var myText = document.getElementById('myText');
    var input_text = document.getElementById('input_text').value  ; //fetches html id = "input_text" and displays value
    var input_date = document.getElementById('input_date').value  ; //fetches html id = "input_date" and displays value
    var input_time = document.getElementById('input_time').value  ; //fetches html id = "input_time" and displays value
        myText.innerHTML += "<li>"+input_text+" "+input_date+" "+input_time+"</li>";}

$(document).ready(function() {
    var myText = $('#myText').val();
    var input_text = $('#input_text').val();   
    var input_date = $('#input_date').val();
    var input_time = $('#input_time').val();
    myText.html() += "<li>"+input_text+" "+input_date+" "+input_time+"</li>";
});




    function wellness () {
        this.task = document.getElementById('input_text').value;
        this.date = document.getElementById('input_date').value;
        this.time = document.getElementById('input_time').value;
    }

//wny does this not let me see all of this in console and in html at the same time?
/*
    function buttonClicked() {
        var answers = new wellness();
        console.log(answers.task);
        console.log(answers.date);
        console.log(answers.time);
    }
*/

/*
now I want to send this data to one of 3 categories on click menu
'critical'
'important'
'future importance'
*/

/*

(document).ready(function() {
    $('wellness').click(function() {
       select & send:
    	    'critical'
			'important'
			'future importance'
    });
});

*/




// what do I learn next? jQuery, bootstrap, or php?
